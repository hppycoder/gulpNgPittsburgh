'use strict';

/**
 * @ngdoc function
 * @name cartApp.controller:Step1Ctrl
 * @description
 * # Step1Ctrl
 * Controller of the cartApp
 */
angular.module('cartApp')
  .controller('Step1Ctrl', function ($sce, $scope, $timeout, $window) {
});
